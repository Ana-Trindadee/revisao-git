# PROJETOS DE REVISÃO DE COMANDOS GIT
* Configurar de forma global o editor de configurações do git.
* Validar comandos de inicialização do repositórito git.

git remote add origin https://github.com/Ana-Trindadee/revisao-git.git
git branch -M main
git push -u origin main